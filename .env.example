# FaultMaven Knowledge Service - Environment Configuration

# ============================================================================
# Vector Database Configuration (Deployment-Neutral)
# ============================================================================
# Vector DB provider: "chroma" (default, local/persistent) or "pinecone" (enterprise cloud)
VECTOR_DB_PROVIDER=chroma

# ChromaDB configuration (when VECTOR_DB_PROVIDER=chroma)
# - Development: Local disk persistence
# - Self-hosted: Docker volume mounts
# - K8s: Persistent volume claims (PVC)
CHROMA_PERSIST_DIR=./data/chroma
CHROMA_COLLECTION_NAME=faultmaven_kb

# Pinecone configuration (when VECTOR_DB_PROVIDER=pinecone)
# - Enterprise: Managed cloud with auto-scaling
# - Multi-region: Global deployment
# PINECONE_API_KEY=pk-xxxxxxxx
# PINECONE_ENVIRONMENT=us-east-1-aws
# PINECONE_INDEX_NAME=faultmaven-kb
# VECTOR_DIMENSION=384

# Embedding Model Configuration
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# ============================================================================
# PostgreSQL Configuration (for document metadata)
# ============================================================================
DATABASE_URL=postgresql+asyncpg://faultmaven:password@localhost:5432/faultmaven_knowledge

# ============================================================================
# Application Settings
# ============================================================================
PORT=8004
LOG_LEVEL=INFO
ENVIRONMENT=development
